const API=(import.meta.env.VITE_API_URL||'http://localhost:8080').replace(/\/$/,'')
const KEY='legal-analyzer:active-job-id'
type Timeline={movimentacoesOficiais:number;eventosPdf:number;correspondencias:number;movimentacoesOcultas:number;linhaDoTempoHibrida:any[];alertasMovimentacoesOcultas:any[];alertasPrazos?:any[];dataPublicacaoOficial?:string|null;dataTransitoEmJulgadoOficial?:string|null;observacao:string}
type Audit={capaEnriquecida:boolean;camposEnriquecidos:string[];validacaoPartesStatus:string;divergencias:string[];observacao:string;dataJud?:{tribunal?:string|null;grau?:string|null;classeProcessual?:string|null;orgaoJulgador?:string|null;encontrado?:boolean}}
function esc(value:any){return String(value??'').replace(/[&<>\"]/g,(c)=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;'}[c]||c))}
export function installDataJudTimelineOverlay(){
 const render=(t:Timeline|null,a:Audit|null)=>{let el=document.getElementById('datajud-timeline-overlay');if(!el){el=document.createElement('section');el.id='datajud-timeline-overlay';document.querySelector('main')?.appendChild(el)};if(!t&&!a){el.innerHTML='';return};const alerts=t?.alertasMovimentacoesOcultas||[];const prazo=t?.alertasPrazos||[];const divergencias=a?.divergencias||[];el.innerHTML=`<div style="margin:24px 0;padding:18px 20px;border:1px solid #dbe4ef;border-radius:14px;background:#fff;box-shadow:0 6px 24px rgba(15,23,42,.06);color:#1e293b"><div style="display:flex;justify-content:space-between;gap:16px;align-items:center;margin-bottom:14px"><b>Auditoria oficial · DataJud</b><span style="font-size:12px;color:#64748b">${a?.capaEnriquecida?'Capa enriquecida':'Consulta oficial'} · ${t?.movimentacoesOficiais||0} movimentações</span></div>${a?.capaEnriquecida?`<div style="display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:10px;margin-bottom:12px">${[['Tribunal',a.dataJud?.tribunal],['Grau',a.dataJud?.grau],['Classe oficial',a.dataJud?.classeProcessual],['Órgão julgador',a.dataJud?.orgaoJulgador]].map(([label,value])=>`<div style="padding:11px 12px;border:1px solid #e5eaf0;border-radius:10px;background:#f8fafc"><b style="display:block;font-size:13px">${esc(value||'—')}</b><small style="display:block;margin-top:3px;color:#64748b">${label}</small></div>`).join('')}</div>`:''}${a?.camposEnriquecidos?.length?`<div style="padding:10px 12px;margin-bottom:10px;background:#f1f5f9;border-radius:10px;font-size:12px"><b>Metadados confirmados pelo DataJud</b><div style="margin-top:4px;color:#475569">${a.camposEnriquecidos.map(esc).join(' · ')}</div></div>`:''}${divergencias.length?`<div style="margin:10px 0;padding:11px 12px;border-radius:10px;background:#fef2f2;border:1px solid #fecaca;font-size:12px"><b>⚠ ${divergencias.length} divergência(s) detectada(s)</b>${divergencias.slice(0,5).map((x:any)=>`<div style="margin-top:4px">${esc(x)}</div>`).join('')}</div>`:''}${alerts.length?`<div style="margin:10px 0;padding:11px 12px;border-radius:10px;background:#fff7ed;border:1px solid #fed7aa;font-size:12px"><b>⚠ ${alerts.length} movimentação(ões) sem correspondente claro no PDF</b>${alerts.slice(0,5).map((x:any)=>`<div style="margin-top:4px">${esc(x.data||'—')} — ${esc(x.descricao||'Movimentação oficial')}</div>`).join('')}</div>`:''}${prazo.length?`<div style="margin:10px 0;padding:11px 12px;border-radius:10px;background:#fffbeb;border:1px solid #fde68a;font-size:12px"><b>⏱ ${prazo.length} possível(is) gerador(es) de prazo</b>${prazo.slice(0,5).map((x:any)=>`<div style="margin-top:4px">${esc(x.data||'—')} — ${esc(x.descricao||x.fase||'Verificar ato oficial')}</div>`).join('')}<small style="display:block;margin-top:7px;color:#64748b">O sistema não presume automaticamente o número de dias do prazo.</small></div>`:''}${t?`<div style="display:flex;flex-wrap:wrap;gap:18px;margin-top:12px;padding-top:12px;border-top:1px solid #e5e7eb;font-size:12px"><span>Correspondências <b>${t.correspondencias}/${t.movimentacoesOficiais}</b></span>${t.dataPublicacaoOficial?`<span>Publicação oficial <b>${esc(t.dataPublicacaoOficial)}</b></span>`:''}${t.dataTransitoEmJulgadoOficial?`<span>Trânsito em julgado <b>${esc(t.dataTransitoEmJulgadoOficial)}</b></span>`:''}</div>`:''}<small style="display:block;margin-top:10px;color:#64748b;line-height:1.5">${esc(a?.observacao||t?.observacao||'')}</small></div>`}
 const poll=async()=>{const id=localStorage.getItem(KEY);if(!id)return;try{const [tr,ar]=await Promise.all([fetch(`${API}/api/v1/processos/analises/${id}/datajud/timeline`),fetch(`${API}/api/v1/processos/analises/${id}/datajud/auditoria`)]);const t=tr.ok?await tr.json():null;const a=ar.ok?await ar.json():null;render(t,a)}catch{}}
 poll();window.setInterval(poll,3000)
}
